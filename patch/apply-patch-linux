#This basically converts the DOS commands into linux compatible commands
#These commands need to be manually run in a terminal within the patch folder
#Feel free to turn it into a functional .sh file

cd ../mc
git checkout master
git apply --ignore-space-change --ignore-whitespace ../patch/minecraft.patch
cp -rf ../mc ../tmp
git reset --hard
git checkout modded
cp -rf ../tmp ../mc
rm -rf ../tmp